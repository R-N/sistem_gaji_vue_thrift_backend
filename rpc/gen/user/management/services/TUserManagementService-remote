#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.20.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py:package_prefix=rpc.gen.
#

import sys
import pprint
if sys.version_info[0] > 2:
    from urllib.parse import urlparse
else:
    from urlparse import urlparse
from thrift.transport import TTransport, TSocket, TSSLSocket, THttpClient
from thrift.protocol.TBinaryProtocol import TBinaryProtocol

from user.management.services import TUserManagementService
from user.management.services.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
    print('')
    print('Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] [-s[sl]] [-novalidate] [-ca_certs certs] [-keyfile keyfile] [-certfile certfile] function [arg1 [arg2...]]')
    print('')
    print('Functions:')
    print('   fetch(string auth_token, TUserQuery query)')
    print('  TUser create(string auth_token, TUserRegistrationForm form)')
    print('  void set_role(string auth_token, i32 user_id, TUserRole new_role)')
    print('  void set_email(string auth_token, i32 user_id, string new_email)')
    print('  void set_password(string auth_token, i32 user_id, string new_password)')
    print('  void set_enabled(string auth_token, i32 user_id, bool new_enabled)')
    print('  void set_verified(string auth_token, i32 user_id, bool new_verified)')
    print('  void delete(string auth_token, i32 user_id)')
    print('')
    sys.exit(0)

pp = pprint.PrettyPrinter(indent=2)
host = 'localhost'
port = 9090
uri = ''
framed = False
ssl = False
validate = True
ca_certs = None
keyfile = None
certfile = None
http = False
argi = 1

if sys.argv[argi] == '-h':
    parts = sys.argv[argi + 1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    argi += 2

if sys.argv[argi] == '-u':
    url = urlparse(sys.argv[argi + 1])
    parts = url[1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    else:
        port = 80
    uri = url[2]
    if url[4]:
        uri += '?%s' % url[4]
    http = True
    argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
    framed = True
    argi += 1

if sys.argv[argi] == '-s' or sys.argv[argi] == '-ssl':
    ssl = True
    argi += 1

if sys.argv[argi] == '-novalidate':
    validate = False
    argi += 1

if sys.argv[argi] == '-ca_certs':
    ca_certs = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-keyfile':
    keyfile = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-certfile':
    certfile = sys.argv[argi+1]
    argi += 2

cmd = sys.argv[argi]
args = sys.argv[argi + 1:]

if http:
    transport = THttpClient.THttpClient(host, port, uri)
else:
    if ssl:
        socket = TSSLSocket.TSSLSocket(host, port, validate=validate, ca_certs=ca_certs, keyfile=keyfile, certfile=certfile)
    else:
        socket = TSocket.TSocket(host, port)
    if framed:
        transport = TTransport.TFramedTransport(socket)
    else:
        transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol(transport)
client = TUserManagementService.Client(protocol)
transport.open()

if cmd == 'fetch':
    if len(args) != 2:
        print('fetch requires 2 args')
        sys.exit(1)
    pp.pprint(client.fetch(args[0], eval(args[1]),))

elif cmd == 'create':
    if len(args) != 2:
        print('create requires 2 args')
        sys.exit(1)
    pp.pprint(client.create(args[0], eval(args[1]),))

elif cmd == 'set_role':
    if len(args) != 3:
        print('set_role requires 3 args')
        sys.exit(1)
    pp.pprint(client.set_role(args[0], eval(args[1]), eval(args[2]),))

elif cmd == 'set_email':
    if len(args) != 3:
        print('set_email requires 3 args')
        sys.exit(1)
    pp.pprint(client.set_email(args[0], eval(args[1]), args[2],))

elif cmd == 'set_password':
    if len(args) != 3:
        print('set_password requires 3 args')
        sys.exit(1)
    pp.pprint(client.set_password(args[0], eval(args[1]), args[2],))

elif cmd == 'set_enabled':
    if len(args) != 3:
        print('set_enabled requires 3 args')
        sys.exit(1)
    pp.pprint(client.set_enabled(args[0], eval(args[1]), eval(args[2]),))

elif cmd == 'set_verified':
    if len(args) != 3:
        print('set_verified requires 3 args')
        sys.exit(1)
    pp.pprint(client.set_verified(args[0], eval(args[1]), eval(args[2]),))

elif cmd == 'delete':
    if len(args) != 2:
        print('delete requires 2 args')
        sys.exit(1)
    pp.pprint(client.delete(args[0], eval(args[1]),))

else:
    print('Unrecognized method %s' % cmd)
    sys.exit(1)

transport.close()
